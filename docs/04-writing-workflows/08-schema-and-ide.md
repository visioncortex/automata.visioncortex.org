---
title: Schema and IDE Support
sidebar_label: Schema & IDE
---

# Schema and IDE Support

ui-automata ships a JSON Schema generated from the same Rust types that power the executor. Add a single comment to any workflow file and your editor will validate your YAML, offer autocomplete for every field, and show inline documentation for every action and condition.

## Adding the schema reference

## VS Code setup

## What the schema validates

## Autocomplete for action types and condition types

## Generating the schema from source
